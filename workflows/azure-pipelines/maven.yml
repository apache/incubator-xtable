trigger:
- main

pr:
- main

stages:
- stage: build
  jobs:
  - job: Linux
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - template: compile-and-test.yml
  - job: macOS
    pool:
      vmImage: 'macos-latest'
    steps:
    - template: compile-and-test.yml
